version: 2

models:
  - name: dim_address
    description: Dimensão endereço da Adventure World 
    columns:
      - name: addressid
        description: chave primária da tabela adress
        tests:
          - unique
          - not_null

      